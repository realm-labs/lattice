# Superseded Implementation Plan

The pre-hard-switch implementation plan has been retired because it described APIs and storage
contracts that no longer exist. The authoritative execution tracker is
[production-hardening-plan.md](production-hardening-plan.md), and the target design is indexed by
[architecture/README.md](architecture/README.md).

The captured pre-migration API, storage, benchmark, and permitted-break baseline is retained in
[baselines/pre-hard-switch.md](baselines/pre-hard-switch.md). It is evidence only and is not a
supported compatibility contract.
